/// Burchak radiuslari — dc.html'dan tasdiqlangan qiymatlar (logical pixel,
/// baza kenglik 393 — `flutter_screenutil` ishlatilmaydi).
class AppRadius {
  static const button = 12.0;

  /// Standart karta (qator kartalari, kit kartasi) — dc.html'da eng ko'p
  /// uchraydigan qiymat.
  static const card = 18.0;

  /// Guruhlangan qator konteyneri (masalan kategoriya ro'yxati o'ramasi).
  static const groupedCard = 16.0;

  static const cardLarge = 22.0;
  static const image = 14.0;
  static const input = 12.0;
  static const pill = 11.0;
  static const sheet = 24.0;
  static const chip = 999.0;
}
